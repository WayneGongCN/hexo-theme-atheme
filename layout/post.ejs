<% if (!page.hideTitle) { %>
  <div>
    <h1 class="fs-2"><%= page.title %></h1>
    <time class="link-secondary"><%= date(page.date, "YYYY-MM-DD") %></time>
  </div>
<% } %>

<main class="markdown-body lh-lg">
  <%- page.content %>
</main>